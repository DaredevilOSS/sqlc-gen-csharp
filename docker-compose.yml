services:
#  plugin-tests:
#    container_name: plugin-tests
#    build: .
#    restart: on-failure
#    env_file:
#      - .env
#    command: dotnet test SqlcGenCsharpTests || exit 1
#    volumes:
#      - .:/SqlcGenCsharp
#    depends_on:
#      mysqldb:
#        condition: service_healthy
        
  mysqldb:
    container_name: mysqldb
    build: examples/authors/mysql
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: tests
      MYSQL_ALLOW_EMPTY_PASSWORD: true
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "127.0.0.1"]
      timeout: 20s
      retries: 10
  