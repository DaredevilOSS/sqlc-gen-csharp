version: '3.8'

services:
  mysqldb:
    build:
      dockerfile: ./mysql.Dockerfile
    restart: always
    network_mode: host
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: tests
      MYSQL_ALLOW_EMPTY_PASSWORD: true